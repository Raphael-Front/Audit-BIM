# Implement "buscar obra" in App Shell

## Overview

Tornar o `ObraSelector` um combobox com busca: ao digitar, a lista filtra as obras por nome (e código), facilitando a seleção quando houver muitas obras. Mantém "Todas as obras" sempre visível no topo.
