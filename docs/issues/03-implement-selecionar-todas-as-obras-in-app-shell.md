# Implement "selecionar todas as obras" in App Shell

## Overview

Adicionar a opção "Todas as obras" como primeira entrada do `ObraSelector`. Ao escolhê-la, `selection = "all"` e `selectedObraId = null`, sinalizando às telas para não aplicarem filtro de obra (visão consolidada). É o estado padrão do sistema.
